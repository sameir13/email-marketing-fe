import React from 'react';
import ListAllCampaigns from '../../features/Campaigns/ListAllCampaigns';

const page = () => {
    return (
        <div>
            <ListAllCampaigns />
        </div>
    );
};

export default page;
