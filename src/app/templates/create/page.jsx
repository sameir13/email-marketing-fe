import React from 'react';
import EmailEditorSection from '../../../features/Templates/EmailEditorSection';

const page = () => {
    return (
        <>
            <EmailEditorSection />
        </>
    );
};

export default page;
